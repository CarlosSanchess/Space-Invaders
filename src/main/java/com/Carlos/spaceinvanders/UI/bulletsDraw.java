package com.Carlos.spaceinvanders.UI;

import com.Carlos.spaceinvanders.Bullet;
import com.Carlos.spaceinvanders.Entities.bulletModel;
import com.googlecode.lanterna.graphics.TextGraphics;

import java.io.IOException;

public class bulletsDraw {
/*
    bulletModel bulletModel;
    BulletsDraw(bulletModel bulletModel){
        bulletModel = this.bulletModel;
    }
    public void draw(TextGraphics graphics)throws IOException {
        //Correr a lista das bullets
        //Mover a bullet
        // desenhar a bullet com o Bullet.draw
        if(!activeBullets.isEmpty() ){
            for(Bullet bullet:activeBullets) {
                bullet.move();
                bullet.isActive();
                if (!bullet.isActive()) {
                    activeBullets.remove(bullet);
                    break;
                }
                bullet.draw(graphics);
            }
        }
    }
    */

}
